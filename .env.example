# Copy this file to .env and fill in your key before running run_pipeline.py locally.
# In GitHub Actions this is injected automatically from the repo secret of the
# same name — you do not need to set it there.

OPENROUTER_API_KEY=your_openrouter_key_here
